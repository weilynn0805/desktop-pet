# 桌面宠物（Desktop Pet）产品需求文档 PRD · 完整版

- 版本：v2.0（基于 v1.0 评审稿重构，补齐战略层与验收层）
- 日期：2026-06-13
- 状态：个人开发指南（可落地执行蓝图）
- 基准文档：`PRD-桌面宠物.md`（v1.0，功能细节以其为准；本版与其决策完全一致）
- 技术栈：Electron（决策 D） · AI 能力：暂不接入

> 本版与 v1.0 的关系：v1.0 已锁定全部产品决策（A–G）与功能细节，本版**不改动任何已拍板结论**，仅按标准 PRD 结构补充：可量化成功指标、用户画像、用户故事 + 验收标准、Non-Goals、分阶段路线图、技术风险登记表。功能实现细节请回查 v1.0 对应章节（文中以 `[v1.0 §x]` 标注）。

---

## 1. 执行摘要（Executive Summary）

### 1.1 问题陈述（Problem Statement）
长时间使用电脑办公/学习的用户缺少一个低打扰、有情感陪伴感的桌面伙伴，同时也缺少与之一体化的"事项提醒"和"基于真实使用时长的健康休息提醒"——现有提醒类工具冰冷、易被忽略，纯桌面宠物又缺乏实用价值。

### 1.2 解决方案（Proposed Solution）
一款常驻桌面、悬浮于所有窗口之上、可拖动的动态虚拟宠物。以"图层叠加"模型统一承载三大功能——**陪伴办公**（基础层）、**防沉迷善意提醒**（覆盖层）、**事项提醒**（最高叠加层），通过控制面板配置素材、时间、文案与提示音，全本地运行、配置持久化。

### 1.3 成功标准（Success Criteria — 可量化 KPI）

| # | 指标 | 目标值 | 验证方式 |
|---|---|---|---|
| K1 | 空闲态资源占用 | 宠物悬浮 + 视频常驻播放时，CPU 平均 ≤ 5%、内存 ≤ 200MB（1080p 以内素材） | 任务管理器 / `process.getProcessMemoryInfo` 采样 1 小时 |
| K2 | 稳定性（崩溃率） | 连续运行 72 小时无崩溃、无内存泄漏（内存增长 ≤ 10%） | 长稳测试 + 内存曲线监控 |
| K3 | 交互准确率 | 单击/拖动判定准确率 100%（移动阈值生效，无误触退出） | 手测用例集（≥ 30 次拖动 / 单击混合操作） |
| K4 | 提醒可靠性 | 设定提醒到点触发误差 ≤ 1 秒；重复规则（每天/每周/工作日）100% 正确 | 自动化时间用例 |
| K5 | 防沉迷计时正确性 | 连续使用满阈值触发、空闲满阈值重置、被提醒打断暂停/恢复，逻辑 100% 正确 | 状态机用例覆盖 §3.3 全部规则 |
| K6 | 场景鲁棒性 | 锁屏/解锁、睡眠/唤醒、显示器插拔下不崩溃、状态正确恢复 | 场景回归用例 |

---

## 2. 用户体验与功能（User Experience & Functionality）

### 2.1 用户画像（Personas）

- **P1 · 长时办公族（核心）**：每天连续用电脑 6h+，需要轻量陪伴 + 健康休息提醒，但反感强制锁屏类工具。重视"不打扰、可随时关"。
- **P2 · 学生/自习党**：用番茄式节奏学习，需要事项提醒（交作业、上网课）+ 防沉迷，喜欢可爱形象与个性化素材。
- **P3 · 个性化玩家**：把桌宠当桌面装饰，重度使用自定义素材/文案/音效，关注观感与缩放自由度。

### 2.2 用户故事与验收标准（User Stories + Acceptance Criteria）

**US-1｜陪伴办公（默认态）** `[v1.0 §4.1, §5.1]`
> 作为长时办公族，我想要一只动态宠物悬浮在桌面上，以便工作时获得陪伴感而不被打扰。
- **AC**：
  - 启动后默认进入陪伴办公态，宠物 always-on-top 悬浮于普通窗口之上。
  - 左键拖动可移动，超过移动阈值才判定为拖动；松手后位置持久化，重启恢复 `[v1.0 §2.2]`。
  - 鼠标悬停：若该素材绑定了文案（≤20字）则头顶显示，移开消失；未配置则不显示。
  - 多素材时按设定时间（5–60 分钟）轮播；单素材时不可设时间。
  - 锁屏时宠物休眠（暂停动画与计时），解锁恢复。

**US-2｜事项提醒** `[v1.0 §4.2, §5.2]`
> 作为学生，我想要在指定时间被宠物提醒，以便不错过重要事项。
- **AC**：
  - 到点时宠物切换为提醒素材，上方弹出提醒面板（白底黑字 + "我知道了"）。
  - 面板**不点不消失**；多条同时到点以可堆叠列表呈现，逐条独立确认。
  - 点"我知道了"后该提醒记为完成，面板消失，宠物恢复底层状态。
  - 支持重复规则：单次 / 每天 / 每周 / 工作日。
  - 提示音可开关，支持内置音效或自定义上传 `[决策 G]`。
  - 时间选择遵循 `[v1.0 §7]`：分 0–59、时 0–23、日按年月动态、禁止选过去时间。

**US-3｜防沉迷善意提醒** `[v1.0 §4.3, §5.3]`
> 作为长时办公族，我想要在连续使用过久时被温和提醒休息，以便保护健康，且能随时关闭。
- **AC**：
  - 基于鼠标/键盘活动检测，**连续使用满阈值（默认 90 分钟）**触发，全屏展示休息素材。
  - 无操作满空闲阈值（默认 10 分钟）/ 锁屏 / 睡眠 → 重置累计计时。
  - 达到设定"休息时长"后自动结束，恢复陪伴办公态。
  - **善意提醒，用户可随时手动关闭/退出程序，不阻止关闭** `[决策 B]`。
  - 独占全屏（游戏/视频）场景下可延迟/不弹出，退出全屏后再处理 `[决策 C]`。

**US-4｜图层协同与打断处理** `[v1.0 §3]`
> 作为用户，我想要三功能互不冲突，以便体验顺畅、无歧义。
- **AC**：优先级 事项提醒 > 防沉迷 > 陪伴办公；提醒为最高叠加层，叠加在防沉迷画面之上；防沉迷倒计时在被提醒打断期间暂停，确认后继续；暂停宠物时提醒与防沉迷计时一并暂停。

**US-5｜控制面板配置与持久化** `[v1.0 §5, §8]`
> 作为个性化玩家，我想要统一面板配置素材/时间/文案/音效，以便自由定制。
- **AC**：双击宠物或右键菜单打开面板；三分区（陪伴/提醒/防沉迷）+ 通用项（开机自启、提示音开关、状态显示/暂停）；所有配置本地持久化、重启恢复；素材支持自由缩放（最大全屏，最小快捷图标大小）。

**US-6｜防误操作交互** `[v1.0 §2.2]`
> 作为用户，我想要不会误退出宠物，以便放心使用。
- **AC**：退出仅经右键菜单的"退出"，单击不退出；右键菜单含 打开面板 / 暂停宠物 / 退出；双击打开面板。

### 2.3 非目标（Non-Goals — 明确不做，保护范围）

- ❌ **不接入 AI 对话/语音**（本期明确排除，见用户决策）。
- ❌ **不做强制防沉迷**：不锁屏、不阻止关闭、不做底层全屏强制穿透 `[决策 B/C]`。
- ❌ **不做素材自动透明化/裁剪/转码处理**：观感由用户负责 `[决策 F]`。
- ❌ **不限制上传素材格式**（仅风险提示，不拦截）`[决策 F]`。
- ❌ **不做多显示器自选屏配置**（本期默认显示在宠物所在屏，后续按需扩展）`[v1.0 §10]`。
- ❌ **不做云同步 / 账号体系 / 联网功能**：纯本地。
- ❌ **不做移动端 / macOS / Linux**：本期仅 Windows 桌面。

---

## 3. AI 系统需求（AI System Requirements）

**不适用（N/A）。** 本期产品为纯本地动画 + 交互 + 定时逻辑，不接入大模型、语音或任何在线 AI 服务（依用户决策）。若未来扩展 AI 闲聊伙伴形态，再单独追加本章（工具需求、评测策略、隐私边界）。

---

## 4. 技术规格（Technical Specifications）

> 技术栈：Electron `[决策 D]`。以下为架构与关键实现点，细节实现要点见 `[v1.0 §9]`。

### 4.1 架构概览（多窗口分离）
四类窗口独立管理，各司其职：

| 窗口 | 职责 | 关键属性 |
|---|---|---|
| 宠物窗 | 渲染动态形象、承载拖动/悬停/点击交互 | `transparent:true`、`frame:false`、`alwaysOnTop:true`、`skipTaskbar:true` |
| 控制面板窗 | 三分区配置 + 通用设置 | 常规窗口 |
| 防沉迷全屏窗 | 覆盖层，全屏休息素材 | 全屏、覆盖宠物所在屏 |
| 提醒面板窗 | 最高叠加层，叠加在任意状态之上 | 最高置顶层级 |

- **渲染**：透明动态形象用 HTML `<video>`（WebM VP9 alpha）或 `<img>`（APNG/GIF）。
- **置顶层级**：`setAlwaysOnTop(true, 'screen-saver')`；不强制穿透独占全屏。
- **拖动**：自实现拖拽 + 移动阈值，区分单击/拖动。
- **状态机**：图层叠加模型（基础层/覆盖层/叠加层），集中管理优先级与打断暂停/恢复 `[v1.0 §3]`。

### 4.2 集成点（Integration Points）
- **系统空闲检测**：`powerMonitor.getSystemIdleTime()`（防沉迷累计与重置）。
- **电源/会话事件**：`powerMonitor` 的 `lock-screen` / `unlock-screen` / `suspend` / `resume`（休眠与计时暂停）。
- **独占全屏检测**：原生模块 / 系统 API 判断前台是否独占全屏。
- **开机自启**：`app.setLoginItemSettings`。
- **持久化**：`electron-store`（配置）+ 本地文件目录（素材路径）。
- **无外部 API / 无数据库 / 无 Auth**（纯本地）。

### 4.3 安全与隐私（Security & Privacy）
- **全本地、无联网、无遥测**：不收集、不上传任何用户数据。
- 素材文件仅存本地用户目录；卸载时提示清理。
- 不申请非必要系统权限；活动检测仅读取系统空闲时长，不记录按键内容（非键盘监听器，仅 idle 时间）。

### 4.4 文件 / 素材约束 `[v1.0 §6]`
- 支持常见视频（mp4/webm/mov）与动图（gif/apng）；单文件 ≤ 50MB、建议 ≤ 1080p，超限提示不拦截。
- ⚠️ 上传界面须提示：普通不透明视频会出现黑/白矩形边框，**推荐透明通道素材**（APNG、透明 GIF、WebM VP9 alpha、HEVC alpha MOV）。

---

## 5. 风险与路线图（Risks & Roadmap）

### 5.1 分阶段路线图（Phased Rollout）

**MVP（核心可用）**
- 透明悬浮宠物窗 + 拖动（含移动阈值）+ 位置记忆。
- 右键菜单（面板/暂停/退出）+ 双击开面板，防误退出。
- 陪伴办公基础态：单/多素材展示 + 悬停文案。
- 控制面板骨架 + 配置本地持久化 + 开机自启。
- 目标：K1（资源占用）、K3（交互准确率）达标。

**v1.1（三功能闭环）**
- 事项提醒：提醒面板、可堆叠列表、重复规则、提示音、时间选择器规则。
- 防沉迷：活动检测计时、阈值触发/空闲重置、全屏休息素材、休息时长自动结束。
- 图层叠加状态机：优先级、打断暂停/恢复、暂停宠物联动。
- 目标：K4、K5 达标。

**v2.0（健壮性与体验打磨）**
- 锁屏/睡眠/显示器插拔全场景鲁棒性；独占全屏检测与抑制弹出。
- 文案可读性（描边/半透明底板）、缩放体验、内存切换释放优化。
- 长稳测试。目标：K2、K6 达标。

**Backlog（后续按需）**：多显示器自选屏、更多内置素材/音效、素材市场。

### 5.2 技术风险登记表（Technical Risks）

| 风险 | 影响 | 缓解措施 |
|---|---|---|
| R1 不透明素材黑/白边框破坏悬浮感 | 体验受损（决策 F 不处理） | 上传界面强提示 + 推荐透明格式 + 提供合规内置素材示例 |
| R2 视频常驻播放导致 CPU/GPU/内存偏高 | 影响 K1/K2 | 限制分辨率/大小、轮播切换释放资源、空闲降帧/暂停渲染策略 |
| R3 独占全屏检测在不同显卡/全屏模式下不准 | 防沉迷误弹出干扰游戏 | 原生 API 多策略判断 + 失败时保守抑制弹出 |
| R4 拖动阈值与单击/双击判定冲突 | 误触退出/误开面板（影响 K3） | 统一手势状态机 + 移动阈值 + 双击时间窗调参 |
| R5 防沉迷计时在锁屏/睡眠/打断下错乱 | 影响 K5 | 集中计时器 + 电源事件钩子 + 状态机用例全覆盖 |
| R6 always-on-top 被某些全屏/独占程序压制 | 提醒不可见 | 提升至 screen-saver 层级；独占全屏按决策 C 延迟处理 |
| R7 长时间运行内存泄漏 | 影响 K2 | 视频元素生命周期管理 + 72h 长稳 + 内存曲线监控 |

---

## 附录 A：与 v1.0 的一致性说明
本版未改动 v1.0 任何已拍板决策（A–G）与功能细节，所有功能实现以 `PRD-桌面宠物.md` 为准。本版新增内容均为结构性补充：成功指标(KPI)、用户画像、标准用户故事与验收标准、Non-Goals、分阶段路线图、技术风险登记表。如二者描述冲突，以 v1.0 的功能结论为准、以本版的结构与验收标准为补充。
